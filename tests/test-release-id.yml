name: Test get release by releaseId
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_REPOSITORY: actions/checkout
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          releaseId: "119603944"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: actions/checkout
      - name: Assert outputs are non-empty
        env:
          TAG_NAME: ${{ steps.action.outputs.tag_name }}
          RELEASE_ID: ${{ steps.action.outputs.id }}
        run: |
          echo "tag_name: $TAG_NAME"
          echo "id: $RELEASE_ID"
          if [ -z "$TAG_NAME" ]; then
            echo "FAIL: tag_name output is empty"
            exit 1
          fi
          if [ "$RELEASE_ID" != "119603944" ]; then
            echo "FAIL: expected id '119603944', got '$RELEASE_ID'"
            exit 1
          fi
          echo "PASS"
