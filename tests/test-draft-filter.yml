name: Test draft filter input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          latest: 'true'
          draft: 'false'
          repo: 'actions/checkout'
          doNotFailIfNotFound: 'true'
        continue-on-error: true
      - name: Assert action ran and returned a release or failed gracefully
        env:
          OUTCOME: ${{ steps.action.outcome }}
          RELEASE_ID: ${{ steps.action.outputs.id }}
          DRAFT: ${{ steps.action.outputs.draft }}
        run: |
          echo "outcome: $OUTCOME"
          echo "release_id: $RELEASE_ID"
          echo "draft: $DRAFT"
          if [ "$OUTCOME" = "success" ]; then
            if [ -n "$RELEASE_ID" ]; then
              if [ "$DRAFT" = "true" ]; then
                echo "FAIL: expected non-draft release but got draft=true"
                exit 1
              fi
              echo "PASS: got non-draft release id '$RELEASE_ID'"
            else
              echo "PASS: no release found (doNotFailIfNotFound=true)"
            fi
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (API not reachable under sentinel repo)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
